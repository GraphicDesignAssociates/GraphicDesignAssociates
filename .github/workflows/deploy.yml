name: Deploy static content to Pages

on:
  push:
    branches: ['main']  # Or your default branch
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      # Configure Node.js environment
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 24.2.0
          cache: 'npm'
      
      #  Install dependencies (clean install to handle cross-platform native modules)
      - name: Install dependencies
        run: |
          rm -rf node_modules package-lock.json
          npm install --legacy-peer-deps

      # Build the project
      # VITE_BASE_PATH controls the deployment base path:
      #   '/' = custom domain (e.g. graphicdesignassociates.com)
      #   '/GraphicDesignAssociates/' = GitHub Pages subdirectory
      # To switch back to subdirectory mode (used with Github pages without Custom DNS), just change VITE_BASE_PATH from '/' to '/GraphicDesignAssociates/'
      - name: Build
        run: npm run build
        env:
          VITE_BASE_PATH: '/'

      # Deploy to GitHub Pages
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: List dist contents (debug)
        run: ls -R dist
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4